# Dockerfile.arm64
FROM docker.gitea.com/act_runner:0.2.13

# Copy ARM64 BuildKit binary
COPY buildkit.tar.gz /tmp/

RUN tar -C /usr/local -xzf /tmp/buildkit.tar.gz && rm /tmp/buildkit.tar.gz

# Ensure buildctl is in PATH
ENV PATH="/usr/local/bin:${PATH}"