name: CI

on:
  push:
    branches:
      - main

jobs:
  test:
    runs-on: pi
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Print message
        run: echo "Hello from Raspberry Pi CI!"
