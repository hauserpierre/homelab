apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: registry
  namespace: registry
spec:
  entryPoints:
    - websecure
  routes:
    - match: Host(`registry.${INTERNAL_DOMAIN}`)
      kind: Rule
      services:
        - name: registry
          port: 5000
  tls:
    secretName: registry-tls
