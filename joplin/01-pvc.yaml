apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: joplin-postgres-pvc
  namespace: joplin
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 20Gi