# IDE / tooling
.idea/
.vscode/
.claude/
CLAUDE.md

# Secrets: never commit real values. Only *-secret.yaml files containing
# <CHANGE_ME> placeholders are tracked; keep real ones as *.local.yaml
*.local.yaml
*.local.env
.env
cluster.env
**/auth
**/htpasswd
*token*.txt
*.key
*.pem
*.crt
kubeconfig*

# OpenTofu / Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log

# doc
docs/
